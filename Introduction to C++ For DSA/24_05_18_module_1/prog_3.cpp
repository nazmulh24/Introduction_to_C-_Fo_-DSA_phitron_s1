#include <bits/stdc++.h>
#include <algorithm> //--> Header
using namespace std;

int main()
{
    // int a = 10;
    // int b = 5;
    // int mi = min(a, b);
    // int ma = max(a, b);

    int mi = min({10, 4, 8, 3, 1});
    int ma = max({10, 4, 8, 3, 1});

    cout << mi << " " << ma << endl;
    return 0;
}
