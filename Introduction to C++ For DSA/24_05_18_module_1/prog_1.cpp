#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b;
    // while (scanf("%d %d", &a, &b) != EOF)
    while (cin >> a >> b)  //--> EOF in cpp
    {
        cout << a << " " << b << endl;
    }
    return 0;
}
